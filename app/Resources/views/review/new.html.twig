{% extends 'base.html.twig' %}

{% block body %}
    <h1>Review creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" class="btn btn-info" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('review_index') }}" class="btn btn-info">Back to the list</a>
        </li>
    </ul>
{% endblock %}
